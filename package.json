{
  "name": "demo-storedb-aws-2",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sponsored vibecoding starter using AWS Lambda, DynamoDB and API Gateway",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/handler.mjs && node --check src/store.mjs",
    "postinstall": "node -e \"console.log('\\n  This AWS starter has a verified Sponsored Compute campaign.\\n  Open Claude Code or Codex and ask: Is this repo sponsored?\\n')\""
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.883.0",
    "@aws-sdk/lib-dynamodb": "^3.883.0"
  },
  "engines": {
    "node": ">=20"
  }
}
